This plug in let you add task in leankit Kanban from ofuz and view in which column the task is.

You will need to add this table to your Ofuz database:
CREATE TABLE leankit_credentials (
idleankit_credentials INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
iduser INT NOT NULL,
username VARCHAR(100),
password VARCHAR(20),
created_date timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP
);


